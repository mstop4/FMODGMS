/// @description FMODGMS_Util_SamplesToSeconds(samples, samplingRate)
/// @param samples
/// @param  samplingRate
function FMODGMS_Util_SamplesToSeconds(samples, samplingRate) {

	// Converts time measured in samples to seconds.

	{
	    return samples / samplingRate;
	}



}
